parser: firewall_syslog
format: syslog
fields: timestamp, action, src_ip, dst_ip, dst_port, protocol, message
normalization: network_event
mapped_fields:
  src_ip: source.ip
  dst_ip: destination.ip
  dst_port: destination.port
