parser: windows_event
format: evtx
fields: timestamp, event_id, provider, user, host, command_line
normalization: endpoint_event
mapped_fields:
  event_id: event.code
  command_line: process.command_line
  asset: host.name
